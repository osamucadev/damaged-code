# Public browser origins allowed by the production BFF.
# Add the temporary App Hosting URL after the backend is created.
CORS_ORIGINS=https://zrp.samuelcaetite.dev

# Production deliberately keeps the existing in-process cache. Do not set
# FIREBASE_ENABLED here unless a future checkpoint explicitly adopts Firestore.
CACHE_TTL_MS=3600000
